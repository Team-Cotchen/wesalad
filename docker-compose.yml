version: '3'

services:
    backend:
        build:
            context: ./backend
        ports:
            - '8000:8000'
    frontend:
        build:
            context: ./frontend
        ports:
          - "80:8080"
        depends_on:
            - backend
